

## Update (~10:38-10:55 UTC) — REAL BUG FOUND: hlops skill was never actually loading in new sessions; fixed the skill root + caught a near-miss secrets leak

**Trigger:** Olivier reported that a brand-new session (different conversation) had zero awareness of the hyperliquid-ops skill and suspected prompt injection when this session referenced it. Correct instinct, real bug — not paranoia, not injection.

**Root cause (verified against actual OpenClaw docs, `docs/tools/skills.md`):** skills only auto-load from a fixed set of roots: `<workspace>/skills`, `<workspace>/.agents/skills`, `~/.agents/skills`, `~/.openclaw/skills`, bundled, or `skills.load.extraDirs`. The hyperliquid-ops skill lived at `/root/.openclaw/workspace/skills-archive/hyperliquid-ops/` — a path I (this agent) chose during the original reconstruction, and "skills-archive" is **not** one of the real skill roots. So the skill was NEVER in the `<available_skills>` block for ANY session, including this one — every "use" of it in this session happened because I manually read files by absolute path, not because the runtime recognized it as a skill. New sessions had no way to know it existed. This had been silently broken since the skill's creation.

**Fix:** `git mv` (preserved history via rename detection) the entire skill from `skills-archive/hyperliquid-ops/` to `skills/hyperliquid-ops/` — the real, highest-precedence workspace skill root. Confirmed via `ls` that the frontmatter (`name`, `description`) is valid and will be picked up by the skills loader on next session start.

**Near-miss caught during the move — Gmail secrets almost got committed to git.** The `.gitignore` had `/skills-archive/hyperliquid-ops/.secrets/` explicitly listed (from the original inbox_check.py setup), but that rule obviously didn't cover the new `skills/hyperliquid-ops/.secrets/` path. The first `git add -A` + commit picked up `gmail.env` (Gmail address + app password) as a tracked file. **Caught before push** (repo was 2 commits ahead of `origin/main`, never pushed) — added the new path to `.gitignore`, ran `git rm --cached` on the secrets file, and amended the commit in place rather than push-then-fix. Verified with `git ls-files | grep -i secret` (empty) that no secret ever left the local repo. Lesson: any future path move for a skill/data directory must double-check `.gitignore` coverage BEFORE the first `git add -A`, not after.

**Cron job payloads also needed updating — they hardcode absolute paths in their prompt text, not just metadata.** All 4 hlops cron jobs' `payload.message` fields had `cd /root/.openclaw/workspace/skills-archive/hyperliquid-ops/scripts` baked into the prompt string itself (this is not stored as a variable — cron jobs literally contain the path as plain text in their instructions). Updated all 4 via `cron update`:
- `hlops-collector` → path fixed, force-ran to confirm (rows=4, addrbook=70, clean).
- `hlops-call-evolve` → path fixed (missed in the first batch update, caught on review, fixed).
- `hlops-contested-flag` → path fixed (long payload, 3-check version).
- `hlops-sunday-retro` → path fixed (not due to fire again until Sunday, so this would have silently broken the weekly retro if left unfixed).

**Also committed:** a fresh live call was placed mid-session (see below) after both prior open calls resolved (both hit their up-targets, confirmed via `hlops-call-evolve`'s forced run — Call 1 target 63,500 vs mark 63,524/63,575; Call 2 target 63,474 vs same mark). New call: ts committed ~10:49 UTC, up=64,579 (the real short-liq magnet from the liq-map, not a guessed level), dn=63,001 (this morning's 4h 0.786 make-or-break, kept as the invalidation per L1 — below the liquidity, not above it), h=24, p_up=0.56, by=Claude. Full THE READ pipeline run first (macro gate CLEAR via dollar-weakness story already on file; wave scenarios; oi_flow — no crowd, turn 0.46 noise-tape; joint state — no D6 trigger armed; analog — leg1 coin-flip, already resolved up, leg2 65% reverse; cross-asset — 5/5 breadth ALIGNED; live pulse — 5m CONFIRMING/CONTINUATION). `call_evolve.py` immediately confirmed it's tracking correctly (OPEN, UNCHANGED, 37% into range).

**Status as of ~10:55 UTC:**
- hlops skill now lives at the correct path and will load automatically in every new session going forward (takes effect on next new session per the skills watcher/snapshot behavior — this session won't retroactively show it in `<available_skills>` since skills snapshot at session start).
- All 4 cron jobs' internal paths fixed and one (collector) live-verified under the new path.
- No secrets ever reached git remote; local repo is clean (`git ls-files | grep secret` empty).
- 3 open ledger calls total historically resolved today (2 hit target, both correctly caught by call_evolve), 1 new call now live and tracked.

**Open follow-up for next session:** verify (from a FRESH session, not this one) that `<available_skills>` now actually lists `hyperliquid-ops` — this session's own system prompt was built before the move and won't show it even though the fix is real. If a fresh session still doesn't see it, something deeper is wrong (e.g. skills watcher not running, or a `metadata.openclaw` gate silently excluding it) and needs re-diagnosis.


## Update (~10:57-11:03 UTC) — credits confirmed back; contested-flag re-check status; memory flush checkpoint

**Billing outage resolved.** User confirmed "credit back" at 10:57 UTC. Verified via direct file/git checks (not chat text) that nothing was lost or corrupted during the gap:
- Git state clean: `git ls-files | grep -i secret` empty, log intact (`66aba3b` → `03f8de0` skill-path-move → `3c27b15` doctrine v3.35), 3 commits ahead of `origin/main`, never pushed, tree clean.
- `.secrets/gmail.env` correctly untracked on disk.
- The exec failure logged at #844 during the outage (a redundant `git rm`/secret-grep verification step) did NOT leave bad state — the actual fix had already landed successfully before that failed call.

**Open item, confirmed still open by direct read (not assumption):** `data/.contested_last_ts` = `1786871688457` (the SECOND-to-last call's ts). `data/calls.json` now has 3 calls total:
1. ts=1786866352316, h=48, up=63500, dn=62150, p_up=0.58, by="Claude+Olivier" — RESOLVED (hit up, confirmed earlier).
2. ts=1786871688457, h=30, up=63474, dn=61900, p_up=0.55, by="Claude" — RESOLVED (hit up, confirmed earlier).
3. ts=1786963783916, h=24, up=64579, dn=63001, p_up=0.56, by="Claude" — the FRESH call committed ~10:49 UTC after running the full read pipeline (macro CLEAR, wave/oi_flow/joint-state/analog/cross-asset/pulse all reviewed). **This is the call `.contested_last_ts` has NOT yet advanced past.**

`hlops-contested-flag`'s real run at 10:54 UTC (before the outage) got cut off mid-task by the billing failure — it logged as "ok" in cron history but the output is just a stub, not a completed 3-check result. It never actually reached call #3. A manual force-run was fired after credits returned but had not completed as of last check (spawns 3 subagents — Grok-4.6 blind audit + GPT-5.1 red-team + Gemini-3-Pro coherence — 2-3 min is normal for this job, not evidence of a stall).

**STATUS AT MEMORY-FLUSH TIME — NOT YET CONFIRMED:** whether the new 3-check `hlops-contested-flag` pipeline (Sonnet-5 orchestration, post path-fix, post Haiku-revert) has ever successfully completed end-to-end against a real call. This is the single most important unverified thing carried into the next session/turn. **Next action: re-check `.contested_last_ts` once (not in a poll loop) — if it now reads `1786963783916`, the check completed and the pipeline is proven live; if still `1786871688457`, either it's still running or it failed again and needs a fresh look at cron run history for that job (job id `857e2c6f-0fd3-49c5-b1f4-0e3ebba076b7`).**

**Also carried forward, unchanged from before:**
- hlops skill now correctly lives at `/root/.openclaw/workspace/skills/hyperliquid-ops/` (real skill root) — takes effect in NEW sessions only; this session's own prompt predates the move. Still needs a fresh-session confirmation that `<available_skills>` actually lists it now.
- All 4 cron job payloads (collector, call-evolve, contested-flag, sunday-retro) had their hardcoded dead path fixed; collector was live-verified clean under the new path. contested-flag's live verification is the one still pending (see above). call-evolve and sunday-retro have not been live-fire-verified under the new path since the fix (call-evolve fired once for the path-fix confirmation per the previous log entry — worth a final glance next session; sunday-retro won't fire again until Sunday 16:00 Madrid, low urgency).


## Update (~11:20-15:20 UTC) — skill-visibility fix CONFIRMED live; doctrine fixes on call framing; real call-evolve spam bug found+fixed; liq-map cross-check; standing BTC call

**Skill-visibility fix: CONFIRMED, closed.** Spawned a genuinely fresh isolated session (taskName: verify_hlops_skill_visible) specifically to check `<available_skills>` without any inherited context. It confirmed hyperliquid-ops now shows up correctly. The path-move fix from the earlier ~10:38 entry is proven live — no longer an open item.

**Doctrine/framing fixes made this session (both applied to the live skill file, not just discussed):**
1. **"2 next most probable moves" reframed as a SEQUENTIAL PATH, not two parallel alternatives.** Olivier caught that I was presenting Move 1 / Move 2 as a fork (either it fades down, or it holds and goes up) when the correct structure is: Move 1 = where it goes first, Move 2 = what happens after that — and each move can carry its own primary/alternate sub-case. Filed as skill proposal `hyperliquid-ops-20260817-fa85954aa6`, then explicitly applied after Olivier said "yes." This is now binding on every future BTC read.
2. **Ledger #085 — horizon-scope discipline.** Root issue behind an apparent "contradiction" between two live calls (#084's swing-desk invalidation below the 62k pool vs. a narrower 4h/1h wave read with dn=63,001) was that neither call stated its own timeframe/horizon loudly. Fix: every call now tags its horizon/call-family explicitly and gets checked against other OPEN calls at the SAME scope before a real contradiction is declared. Different horizons giving different levels is not a contradiction; same-scope disagreement is.
3. **Self-caught process failure, logged directly (not hidden):** within this session I flipped the SAME open call's primary direction twice without flagging it as a delta — 11:25 UTC framed Move 1 as fade-down/Move 2 as hold-up; 11:57 UTC silently reversed to Move 1 push-up/Move 2 reject-down, driven by a real but short-lived 5m volume burst (+965%) that reverted to FADING/DIVERGING within ~4 minutes. Doctrine's own continuity rule (§7) requires stating a directional flip as an explicit delta from the prior read, not restating a different order as if it were always the read. Corrected by 12:01 UTC re-pulse showing the burst had already died; reverted to down-first as primary, matching the desk's actual historical #083/#084 pattern (down-poke-then-bounce), which Olivier independently flagged from memory before I'd re-checked.

**Real infra bug found and fixed: `hlops-call-evolve` duplicate-notification spam.** The cron re-fired the identical "Call 1 / Call 2 RESOLVED (hit up)" message roughly every 15 minutes for ~2.5 hours (12:46→15:16 UTC, 10+ duplicates) because `data/calls.json` was never pruned after those two calls first resolved (~13:46) — the script has no archive/removal step, so it re-detects RESOLVED on every run and re-notifies. Root cause confirmed by reading `data/call_evolution.jsonl` directly. **Fix applied:** removed both resolved calls from `data/calls.json`, leaving only the live 24h call (ts=1786963783916, up=64579, dn=63001, p_up=0.56). Cron should stop duplicate pings on its next run (~15:32 UTC) — not yet independently re-verified as of this writing; worth a glance next session.

**`hlops-contested-flag` pipeline — confirmed completed successfully (closes the other open item from the ~11:03 entry).** `.contested_last_ts` now correctly reads `1786963783916`, matching the current live call. A full GPT-5.1 red-team pass completed against it: found a real short cohort building via negative/worsening premium divergence, sized ~$3.1M liq cluster at 64,252-64,888, read as bullish context (not against the call). Grok blind-audit + Gemini coherence also ran clean in that cycle. Two 402 billing failures did hit this job during the outage window (13:54, 14:24) but a clean run completed after.

**New observation family cross-check: Coinglass liq-map (P2) vs HL-only collector (venue-local, thin sample).** Olivier supplied a Coinglass liquidation heatmap screenshot showing a real long-liq cliff at **61,868.74**. The HL-address-scan collector (only ~115-124 HL addresses) had shown nothing notable there — a genuine blind spot, not a bug: per doctrine #27 the P2 liq-map layer is interactive/browser-only and structurally can't be reached by scheduled/headless cron firings. Cross-checked against `structure.py`'s own mechanical output: **61,645–61,880 independently shows up as its own confluence zone (score 84, TL-support + swing-low, 1d/1h)** — so this level has both liq-map AND independent technical confirmation, not just size. Olivier asked whether this makes it a HIGHER-probability target than the 62,520-62,730 shelf; pushed back correctly using doctrine rule #24 (T-002/T-003: "bigger pool = price preferentially travels there" was explicitly tested and REJECTED, 0/4 tests, leverage is dynamic/not static-radius-predictable) — kept 62,520-62,730 as primary (score 469/390, stronger, more timeframes agree) with 61,868 upgraded to a stronger secondary/nested target, not automatically the higher-probability one just because the pool is bigger.

**Standing BTC call as of 15:16 UTC (Olivier explicitly confirmed "perfect i like it and agree" on this exact framing — treat as the live reference):**
- Move 1 (~55%): down-poke into **62,520-62,730** (primary shelf, strongest confluence) → if that fails to reclaim, secondary/nested magnet at **61,868** (liq-map + structure confirmed). Invalidation: hourly acceptance below ~62,000 with no reclaim.
- Move 2 (~58%, conditional on Move 1): reclaim/bounce to **63,474-63,500**, then **65,475-65,968** on further acceptance. Invalidation: fails to reclaim, accepts <62,000.
- Tripwire: 63,001.
- **IMPORTANT CAVEAT for next session: price did NOT follow this sequence.** By 15:16 UTC mark had moved to ~63,840-63,875 — i.e. it broke straight up through the 63,001 tripwire toward the Move-2 zone WITHOUT the Move-1 down-poke happening first. Flow (5m/1h) stayed FADING/DIVERGING even as price climbed — same fake-push signature seen repeatedly today, unresolved. This needs an explicit fresh read/delta next session, not a silent continuation of the stale down-first framing.

**Also carried a ~10-minute outage gap (16:17-16:31ish window not logged, unrelated 402 billing errors) with no data loss — confirmed via direct git/file checks each time, not assumed clean.**

**Open follow-ups for next session:**
1. Verify `hlops-call-evolve` actually stopped the duplicate RESOLVED pings after the `calls.json` prune (check cron run history post-15:32 UTC).
2. Give BTC a fresh full read reconciling the standing call against the fact that price already broke the tripwire and is sitting in/near the Move-2 zone without the expected Move-1 leg — state it explicitly as a delta per the continuity-rule fix from this session, not a silent restart.
3. `hlops-sunday-retro` path fix still not live-fire-verified (won't trigger until Sunday 16:00 Madrid) — low urgency, just don't forget it's unverified.
